/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Output standalone build for efficient production deployment
  output: 'standalone',
};

module.exports = nextConfig;
